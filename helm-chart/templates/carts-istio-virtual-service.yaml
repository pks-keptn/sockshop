apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: sockshop-dev-carts
spec:
  hosts:
  - "carts.sockshop-dev.workshop01.pks.gcp.aklabs.io"
  gateways:
  - sockshop-dev-gateway
  http:
    - route:
      - destination:
          host: carts.sockshop-dev.svc.cluster.local
